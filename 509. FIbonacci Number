class Solution {
    public static int fib(int n) {

        int n1 = 0, n2 = 1, next;

        if (n == 0)
            return n1;

        for (int i = 0; i < n; i++) {
            next = n1 + n2; 
            n1 = n2; 
            n2 = next; 
        }

        return n1; 

    }
}
